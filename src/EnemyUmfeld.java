import client.game.factory.PlayerControllerLoader;
import client.gui.match.PlayerInfoPanel;
import core.Vector;
import core.ai.AiMapInfo;
import core.ai.AiPlayerInfo;
import core.player.Player;
import core.player.PlayerController;

//public class EnemyUmfeld {
//	//Constants
//	private Vector longestDist = new Vector (Float.MAX_VALUE,Float.MAX_VALUE);
//	//Nearest Enemy Vector
//	public Vector getNearestEnemy(AiMapInfo map, AiPlayerInfo ownPlayer){
//		Vector nextEnemy = new Vector(0,0);
//		Vector tempEnemy = longestDist;
//		Player enemys = new Player(AiPlayerInfo., index)
//		
//		for(int i = 0; i < Player.PLAYER_COLORS.length; i++){
//			if(!(map.getFlags()[i].isOwner(ownPlayer))){
//				nextEnemy = .getPosition().sub(ownPlayer.getPosition());
//				if(nextFlag.length() < tempFlag.length()){
//					tempFlag = nextFlag;
//				}
//			}
//		}
//		return tempFlag;
//	}
//}
